# clustering-hands-on
This was a interesting project for this week, but it gave me more insight in understanding unsupervised learning works and how clustering algorithms. I used the Iris dataset which allowed me to see how different flower measurements. After testing several different K values iwth K-Means, I found that K = 3 produced some of the best results for me because of the different Irises Setosa, versicolor and Virginica. DBSCAN revealed group data points through values of eps and the min_samples. Using DBSCAN, I could showed mmall changes to those parameters produced noticeably different clustering results, which demonstrated the importance of tuning the model. Overall, this assignement helped me understand both the strengths and limitatons of K-Means and DBSCAN. It also showed that choosing the right algorithm depends on the dataset and the problem being solved
